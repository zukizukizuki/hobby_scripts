# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}


# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}

# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
# main.tf

terraform {
  required_version = ">= 0.14.0"
  required_providers {
    custom = {
      source  = "example.com/examplecorp/custom"
      version = "~> 1.0"
    }
  }
}

provider "custom" {
  # プロバイダーの設定
  api_key = var.custom_api_key
}

resource "custom_resource" "example" {
  name = "example-resource"
  # リソースの設定
}

module "custom_module" {
  source = "./modules/custom"
  # モジュールの入力変数
}

variable "custom_api_key" {
  type        = string
  description = "API key for the custom provider"
}

output "resource_id" {
  value = custom_resource.example.id
}
